<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/_header') %>
</head>
<body>
	<ul>
	    <% for (url in urls) { %>
			<li><%= urls[url] %> | <a href="/urls/<%= url %>">Update</a>
				<form action="<%= "/urls/" + url + "/delete" %>" method="post"><button>Delete</button></form>
			</li>
	    <% } %>
    </ul>
</body>
</html>